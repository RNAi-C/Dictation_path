# Pathology Dictation Assistant - Phase 1
# Windows 11 + NVIDIA GPU (CUDA)

# Core dependencies
faster-whisper==1.0.2
numpy==1.24.3
scipy==1.11.1

# Audio I/O
sounddevice==0.4.6
soundfile==0.12.1

# Hotkey and system integration
keyboard==0.13.5
pyperclip==1.8.2

# Configuration and utilities
pydantic==2.4.2
python-dotenv==1.0.0

# Optional: For better logging
loguru==0.7.2

# Optional: For system tray integration (Phase 2)
# pystray==0.19.4
# Pillow==10.0.0

# Optional: AI rewrite/cleanup feature (install separately, CPU-first)
# Requires placing .gguf model files in  models/rewrite/
# CPU install:
#   pip install llama-cpp-python
# GPU (CUDA 12.1) install:
#   pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121
# llama-cpp-python>=0.2.85
